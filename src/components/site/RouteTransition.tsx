/**
 * Page transitions are now handled by a pure content cross-fade applied in
 * `Layout.tsx` (see `animate-page-in`). The full-screen curtain was removed
 * for a faster, more elegant perceived navigation.
 */
export const RouteTransition = () => null;
