# Project Memory

## Core
Reference style: landipappalardoavvocati.com — clean editorial sans-serif, no italics, no decorative gold underlines/dashes/rules.
Typography: Inter / Source Sans 3 only. `--font-serif` is aliased to sans; never reintroduce a real serif font without asking.
Never use `italic` / `<em>` / `<i>` for styling — global CSS reset forces `font-style: normal`.
Nav links: plain text, no `::after` underline animation. Hover = color shift to `gold-deep`.
Eyebrows: uppercase tracked text only, no leading gold dash. No `gold-rule`, no `gold-corner` decorations.
Brand colors stay: primary `#14213D` deep blue, accent `#FDA311` yellow ("gold" tokens). All HSL.
Firm: Studio Legale Accarino · Salerno · Est. 1974. Contacts are [PLACEHOLDER] until provided.
